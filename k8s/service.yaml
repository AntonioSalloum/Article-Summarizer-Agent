apiVersion: v1
kind: Service
metadata:
  name: article-summarizer-agent
spec:
  selector:
    app: article-summarizer-agent
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer